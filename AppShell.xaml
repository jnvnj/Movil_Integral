<?xml version="1.0" encoding="UTF-8" ?>
<Shell
    x:Class="BallonParty.AppShell"
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:local="clr-namespace:BallonParty"
    Shell.FlyoutBehavior="Disabled"
    Title="BallonParty">

    <ShellContent Title="Login" ContentTemplate="{DataTemplate local:LoginPage}" />
    <ShellContent Title="Main" ContentTemplate="{DataTemplate local:MainPage}" />
    <ShellContent Title="Contact" ContentTemplate="{DataTemplate local:ContactPage}" />
</Shell>